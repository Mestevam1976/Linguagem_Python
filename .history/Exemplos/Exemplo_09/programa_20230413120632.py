# Vetores em Python

N: int

N = int(input("Quantos numeros voce vai digitar? "))

#vet: float[i] = [0 for x in range(N)]

vet = float[]
for i in range(0, N):
    vet[i] = float(input("Digite um numero: "))

print()
print("NUMEROS DIGITADOS:")
for i in range(0, N):
    print(f"{vet[i]:.1f}")